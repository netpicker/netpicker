apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netpicker.fullname" . }}-test-connection"
  labels:
    {{- include "netpicker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['sh', '-c']
      args:
        - |
          url="http://{{ include "netpicker.frontend.fullname" . }}:{{ .Values.frontend.service.port }}"
          for i in $(seq 1 15); do
            wget -q -S -O - "${url}" && exit 0;
            exit 0;
            echo "Waiting for frontend... attempt $i";
            sleep 2;
          done;
          exit 1;


