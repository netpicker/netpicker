apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netpicker.fullname" . }}-test-connection"
  labels:
    {{- include "netpicker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netpicker.fullname" . }}:{{ .Values.frontend.service.port }}']
  restartPolicy: Never
